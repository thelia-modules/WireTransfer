<?xml version="1.0" encoding="UTF-8"?>
<!--
    Routes declared in XML so they register on BOTH Thelia 2 (annotation router reads XML/
    docblocks, not PHP attributes) and Thelia 3 (which also loads module Config/routing.xml).
    Do NOT also add #[Route] attributes on the controller: Thelia 3 would then register the
    route twice.
-->
<routes xmlns="http://symfony.com/schema/routing"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://symfony.com/schema/routing http://symfony.com/schema/routing/routing-1.0.xsd">

    <route id="wiretransfer.configure" path="/admin/wiretransfer/configure" methods="post">
        <default key="_controller">WireTransfer\Controller\ConfigureController::configure</default>
    </route>
</routes>
